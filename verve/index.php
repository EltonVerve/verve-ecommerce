<?php
/**
 * ROOT LOADER
 * ---------------------------------------------------------
 * This file exists ONLY so the site works at a clean URL
 * (yoursite.com/ or yoursite.com/index.php). It has no real
 * code of its own — the actual homepage lives in pages/home.php,
 * same as every other page lives in pages/.
 * ---------------------------------------------------------
 */
require __DIR__ . '/pages/home.php';
